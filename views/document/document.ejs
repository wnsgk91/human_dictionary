<!DOCTYPE html>
<html>
    <head>
        <% include ../header.ejs %>
    </head>
    <body>

        <div class="jumbotron jumbotron-fluid">
            <div class="container"><br/><br/>
                <h1 class="display-4"><%= names[0].name %></h1>
                <p class="lead">This is a modified jumbotron that occupies the entire horizontal space of its parent.</p>
                <p id="Dapibus ac facilisis in">
                    <button type="button" class="btn btn-outline-info" id="add">즐겨찾기 추가</button>
                    <button type="button" class="btn btn-outline-info" id="rmv">즐겨찾기 삭제</button>
                </p>
            </div>
        </div>

            <div class="card text-center">
                <div class="card-header">
                    <ul class="nav nav-tabs card-header-tabs">
                        <li class="nav-item active"><a class="nav-link active" href="/document/<%= names[0].name %>/<%= contents[0].sum %>">개요</a></li>
                        <li class="nav-item active"><a class="nav-link active" href="/document/<%= names[0].name %>/<%= contents[0].symptom %>">증상</a></li>
                        <li class="nav-item active"><a class="nav-link" href="/document/<%= names[0].name %>/<%= contents[0].cause %>">원인</a></li>
                        <li class="nav-item active"><a class="nav-link" href="/document/<%= names[0].name %>/<%= contents[0].diagnosis %>">진단</a></li>
                        <li class="nav-item active"><a class="nav-link" href="/document/<%= names[0].name %>/<%= contents[0].treat %>">치료</a></li>
                        <li class="nav-item active"><a class="nav-link" href="/document/<%= names[0].name %>/<%= contents[0].site %>">참고자료</a></li>
                        <li class="nav-item active"><a class="nav-link" href="/document/<%= names[0].name %>/<%= contents[0].institution %>">의료기관</a></li>
                        <li class="nav-item active"><a class="nav-link" href="/document/<%= names[0].name %>/<%= contents[0].supervisor %>">감수</a></li>
                        <li class="nav-item active"><a class="nav-link" href="/document/<%= names[0].name %>/<%= contents[0].etc %>">기타</a></li>
                    </ul>
                </div>
                <div class="card-body">
                    <h5 class="card-title"><%=  %></h5>
                    <p class="card-text"><%=  %></p>
                </div>
            </div>
    </body>
</html>